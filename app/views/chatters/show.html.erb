

<div class="jumbotron">
  <h1 class="display-4"> <%= @chatter.first_name %> <%= @chatter.last_name %></h1>
  <p class="lead"> <%= @chatter.email %> | <%= @chatter.phone %></p>
</div>

  <%= link_to "Edit", edit_chatter_path(@chatter), class:"btn btn-primary" %> |
  <%= link_to "Back to chatters", chatters_path, class:"btn btn-info" %>

  <br><br>
  <%= button_to "Destroy this chatter", @chatter, method: :delete, class:"btn btn-danger" %>
</div>
